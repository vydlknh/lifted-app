[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  
[[functions]]
  functions = "netlify/functions/*"
  node_bundler = "esbuild"
  
[build.environment]
  NODE_VERSION = "18"

[dev]
  command = "npm run dev"
  framework = "#custom"
  targetPort = 5173
  port = 8888